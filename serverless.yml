service: wishy

provider:
  name: aws
  runtime: go1.x
  region: ${env:AWS_REGION, 'eu-west-1'}
  stage: ${env:STAGE, 'dev'}

functions:
  lambda:
    handler: main
    events:
      #- http:
      #   path: /categories
      #    method: ANY
      - http:
          path: /wishes
          method: GET

